// javascript:document.body.contentEditable='true'; document.designMode='on'; void 0

void f(int []);
struct colors{
char *p;
char *q;
char *s;
};
funct(struct colors t){
	t.p = "Pink";
	printf("%s %s %s", t.p, t.q, t.s);
}
main(){

unsigned int a=-1;
int b=-1;
printf("%u %u", a, a*b);

//int arr[10];
//printf("%u %u", &arr, &(arr + 1));


 
/*	struct colors temp;
	temp.p = "Red";
	temp.q = "Blue";
	temp.s = "Green";
	funct(temp);
	printf("%s %s %s", temp.p, temp.q, temp.s);*/

			
			/*int a[]={1,2,3};
f(a);
printf("%d", *(a+1));*/

/*char buff[] = "this is a test";
int i, *ptr; 
i= i+ +2;
ptr = (int*)buff;
for (i=0;*ptr; i++);
printf("%c",*ptr++);

int (*(*pr)(int)) (void);*/

/* int x=0,t=0;
       switch(x)
       {
       case 0: x++;
       do {

       case 1:t++;
       case 2:t++;
       case 3:t++;

       x++;
       } while(x<=2);

       }
       printf("%d\n",t); */
       getch();
}
void f(int p[]){
     p[1] = 15;
     }
